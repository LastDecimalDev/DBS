import { FormBuilderDirective } from './form-builder.directive';

describe('FormBuilderDirective', () => {
  it('should create an instance', () => {
    const directive = new FormBuilderDirective();
    expect(directive).toBeTruthy();
  });
});
